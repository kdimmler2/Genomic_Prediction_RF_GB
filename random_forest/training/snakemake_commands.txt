snakemake -s data_preprocessing.smk \
    --cores 4 \
    --printshellcmds \
    --rerun-incomplete \
    --configfile config.yaml \
    --keep-going \
    --rerun-triggers mtime

snakemake -s RF_hyperparameter_tuning.smk \
    --cores 4 \
    --printshellcmds \
    --rerun-incomplete \
    --configfile config.yaml \
    --keep-going \
    --rerun-triggers mtime
    
snakemake -s RF_threshold_tuning.smk \
    --cores 4 \
    --printshellcmds \
    --rerun-incomplete \
    --configfile config.yaml \
    --keep-going \
    --rerun-triggers mtime
    
snakemake -s RF_test_metrics.smk \
    --cores 4 \
    --printshellcmds \
    --rerun-incomplete \
    --configfile config.yaml \
    --keep-going \
    --rerun-triggers mtime
    
snakemake -s RF_final_training.smk \
    --cores 4 \
    --printshellcmds \
    --rerun-incomplete \
    --configfile config.yaml \
    --keep-going \
    --rerun-triggers mtime
